syntax = "proto3";

package dispatch.sdk.v1;

import "google/protobuf/any.proto";

// List is a list of google.protobuf.Any values.
message List {
  repeated google.protobuf.Any items = 1;
}

// Map is a map of key/value pairs.
message Map {
  repeated MapItem items = 1;
}

// MapItem is a key/value pair in a map.
message MapItem {
  google.protobuf.Any key = 1;
  google.protobuf.Any value = 2;
}
